DISCORD_TOKEN=replace_with_your_bot_token

# Optional. If set, slash commands sync only to this guild (instant).
# Leave blank for global sync (can take up to an hour to propagate).
DISCORD_GUILD_ID=
